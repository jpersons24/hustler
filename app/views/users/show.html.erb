<h3>This is <%= @user.name %>'s page!</h3>

<img src="<%= @user.photo %>" alt="<%= @user.name %>">

<h5>Bio: <%= @user.bio %></h5>

<h5>Reviews:</h5>

<% if @user.reviews.length < 1 %>
    <p>No Reviews by this User yet!</p>
<% else %>
    <ul>
        <% @user.reviews.each do |r| %>
            <li><%= r.title %></li><br>
            <li><%= r.content %></li>
        <% end %>
    </ul>
<% end %> 

<br><br>

<%= button_to 'Edit User', edit_user_path(@user), method: :get %>

<br><br><br>

<%= button_to "New Task", new_task_path, method: :get %>

<br><br>

<div>
    <p><i><%= @user.name %> task history:</i></p>
        <ul>
            <% @user.tasks.each do |task| %>
                <li><%= task.worker %></li>
                <li><%= task.skill.name %> | <%= task.skill.category %></li>
                <li><%= task.description %></li>
                <li><%= task.cost %></li>
                <li><%= task.deadline %></li>
                <li><%= task.accepted %></li>
                <li><%= task.completed %></li>
            <% end %>
        </ul>
</div>

<br><br>

<p>Thinking about deleting this User?</p>
<%= button_to "Delete User",  user_path(@user), method: :delete%>
